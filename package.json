{
  "name": "openpets-plugin-deadline-buddy",
  "version": "1.0.0",
  "description": "Manual deadlines and selective read-only Google Calendar and Outlook event tracking for OpenPets.",
  "type": "module",
  "files": [
    "index.js",
    "locales/",
    "openpets.plugin.json"
  ],
  "scripts": {
    "test": "node test.js",
    "validate": "npx --yes @open-pets/cli plugin validate ."
  },
  "devDependencies": {
    "@open-pets/plugin-sdk": "^3.0.0"
  }
}
